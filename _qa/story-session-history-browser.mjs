import assert from 'node:assert/strict'
import { spawn } from 'node:child_process'
import { mkdtemp, mkdir, rm } from 'node:fs/promises'
import { tmpdir } from 'node:os'
import { dirname, join, resolve } from 'node:path'
import { fileURLToPath } from 'node:url'
import { chromium } from '/Users/yin/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs'

const root=resolve(dirname(fileURLToPath(import.meta.url)),'..'); const databaseDirectory=await mkdtemp(join(tmpdir(),'draw-me-out-history-ui-')); const evidenceDirectory=resolve(root,'_qa/ui'); await mkdir(evidenceDirectory,{recursive:true})
const requestedSuffix=process.env.STORY_LAB_UI_EVIDENCE_SUFFIX??''; const evidenceSuffix=/^-[a-z0-9-]+$/.test(requestedSuffix)?requestedSuffix:''
const child=spawn(process.execPath,['--import','tsx','_qa/run-story-session-ui.ts'],{cwd:root,env:{...process.env,STORY_LAB_UI_DATABASE_DIR:databaseDirectory,STORY_LAB_UI_PORT:'5197'},stdio:['ignore','pipe','pipe']})
let serverLog=''; child.stderr.on('data',chunk=>{serverLog+=String(chunk)})
const ready=new Promise((resolveReady,rejectReady)=>{let buffer='';const timeout=setTimeout(()=>rejectReady(new Error(`UI lab timeout\n${serverLog}`)),20_000);child.stdout.on('data',chunk=>{buffer+=String(chunk);for(const line of buffer.split('\n'))try{const value=JSON.parse(line);if(value?.url){clearTimeout(timeout);resolveReady(value.url);return}}catch{}});child.once('exit',code=>{clearTimeout(timeout);rejectReady(new Error(`UI lab exited ${code}\n${serverLog}`))})})
let browser
const stopChild=async()=>{if(child.exitCode!==null)return;child.kill('SIGTERM');await Promise.race([new Promise(resolveExit=>child.once('exit',resolveExit)),new Promise(resolveTimeout=>setTimeout(resolveTimeout,5_000))]);if(child.exitCode===null)child.kill('SIGKILL')}
try{
  const labUrl=await ready; const origin=new URL(labUrl).origin; browser=await chromium.launch({headless:true})
  const output=page=>page.locator('output[data-story-session-test]')
  const waitIdle=async page=>{await output(page).waitFor({state:'attached'});await page.waitForFunction(()=>document.querySelector('output[data-story-session-test]')?.getAttribute('data-busy')==='false')}
  const resume=async page=>{const button=page.locator('.st-resume-dialog__primary');if(await button.count())await button.click()}
  const enter=async page=>{const button=page.locator('button.st-primary');if(await button.count()){await button.click();await waitIdle(page);const phase=await page.locator('main[data-turn-phase]').getAttribute('data-turn-phase');if(phase!=='result')throw new Error(`entry did not reach result: ${JSON.stringify({phase,scene:await output(page).getAttribute('data-scene'),version:await output(page).getAttribute('data-version'),blocked:await output(page).getAttribute('data-blocked'),error:await page.locator('[data-story-error]').allTextContents()})}`)}}
  const advanceResult=async page=>{
    assert.equal(await page.locator('.st-quick-replies button').count(),0,'choices stay hidden during result')
    for(let index=0;index<10;index+=1){const next=page.locator('.ct-result-story button');if(!(await next.isVisible()))break;await next.click()}
    const reveal=page.locator('.ct-turn-next');await reveal.waitFor();await reveal.click();await page.locator('main[data-turn-phase="decision"]').waitFor()
    for(let index=0;index<10;index+=1){const next=page.locator('.ct-stage__caption-page');if(!(await next.isVisible()))break;await next.click()}
    await page.locator('.st-quick-replies button').first().waitFor()
  }
  const openHistory=async page=>{await page.locator('button.st-world-button').click();await page.locator('.st-drawer-tabs button').last().click();await page.locator('.st-log button').last().click();await page.locator('.st-session-history__list button').first().waitFor()}
  const control=update=>fetch(`${origin}/__story_lab/control`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(update)})

  const wide=await browser.newContext({viewport:{width:390,height:844},deviceScaleFactor:1}); const page=await wide.newPage(); await page.goto(`${labUrl}?run=history&actor=qa-a&lang=zh`);await waitIdle(page);const original=await output(page).getAttribute('data-session-id');await enter(page);assert.equal(await output(page).getAttribute('data-scene'),'1');assert.equal(await output(page).getAttribute('data-cursor'),'1');await advanceResult(page)
  await page.screenshot({path:join(evidenceDirectory,`platform-layout-draw-me-out-session-decision-zh-390x844${evidenceSuffix}.png`),fullPage:true});await openHistory(page);assert.equal(await page.locator('.st-session-history__list button').count(),1)
  await page.locator('.st-world-restart__open').click();await page.locator('.st-world-restart__confirm .is-danger').click();await page.waitForFunction(previous=>document.querySelector('output[data-story-session-test]')?.getAttribute('data-session-id')!==previous,original);await waitIdle(page);const restarted=await output(page).getAttribute('data-session-id');assert.notEqual(restarted,original);await enter(page);await advanceResult(page);await openHistory(page)
  const rows=page.locator('.st-session-history__list button');assert.equal(await rows.count(),2);for(let i=0;i<await rows.count();i++){const box=await rows.nth(i).boundingBox();assert.ok(box&&box.height>=44&&box.width>=44)}
  await page.locator('.st-session-history').evaluate(node=>node.scrollIntoView({block:'start'}));await page.screenshot({path:join(evidenceDirectory,`platform-layout-draw-me-out-session-history-zh-390x844${evidenceSuffix}.png`),fullPage:true});await page.locator('.st-session-history__list button:not([aria-current="true"])').click();await page.waitForFunction(expected=>document.querySelector('output[data-story-session-test]')?.getAttribute('data-session-id')===expected,original);await waitIdle(page);await resume(page)

  const recovery=await wide.newPage();await recovery.goto(`${labUrl}?run=recovery&actor=qa-a&lang=zh`);await waitIdle(recovery);await enter(recovery);await advanceResult(recovery);await control({dropAndBlock:true});await recovery.locator('.st-quick-replies button').first().click();await waitIdle(recovery);assert.equal(await output(recovery).getAttribute('data-blocked'),'true');assert.ok(await output(recovery).getAttribute('data-pending-id'));assert.equal(await output(recovery).getAttribute('data-version'),'1','unknown response must not optimistically render')
  await recovery.reload();await waitIdle(recovery);assert.equal(await output(recovery).getAttribute('data-blocked'),'true');assert.ok(await output(recovery).getAttribute('data-pending-id'));await resume(recovery);await control({apiUnavailable:false});await recovery.locator('[data-story-error]:visible button').click();await recovery.waitForFunction(()=>document.querySelector('output[data-story-session-test]')?.getAttribute('data-blocked')==='false');await waitIdle(recovery);assert.equal(await output(recovery).getAttribute('data-version'),'2');assert.equal(await output(recovery).getAttribute('data-pending-id'),'');await recovery.locator('main[data-turn-phase="result"]').waitFor();assert.equal(await recovery.locator('.st-quick-replies button').count(),0,'recovered committed result is shown before next choices')

  const ending=await wide.newPage();await ending.goto(`${labUrl}?run=ending&actor=qa-a&lang=zh&finale=ready`);await waitIdle(ending);await resume(ending);await ending.locator('.st-ending-gate button').waitFor();await control({dropAndBlock:true});await ending.locator('.st-ending-gate button').click();await waitIdle(ending);assert.equal(await output(ending).getAttribute('data-blocked'),'true');assert.ok(await output(ending).getAttribute('data-pending-ending-id'));assert.equal(await output(ending).getAttribute('data-finale'),'ready')
  await ending.reload();await waitIdle(ending);await resume(ending);assert.ok(await output(ending).getAttribute('data-pending-ending-id'));await control({apiUnavailable:false});await ending.locator('[data-ending-error]').waitFor();await ending.locator('.st-ending-gate button').click();await ending.waitForFunction(()=>document.querySelector('output[data-story-session-test]')?.getAttribute('data-finale')==='complete');await waitIdle(ending);assert.equal(await output(ending).getAttribute('data-pending-ending-id'),'');assert.equal(await output(ending).getAttribute('data-version'),'28');assert.equal(await output(ending).getAttribute('data-cursor'),'0');await ending.locator('.st-ending').waitFor();await ending.screenshot({path:join(evidenceDirectory,`platform-layout-draw-me-out-ending-recovered-zh-390x844${evidenceSuffix}.png`),fullPage:true})

  const actorB=await wide.newPage();await actorB.goto(`${labUrl}?run=history&actor=qa-b&lang=zh`);await waitIdle(actorB);await enter(actorB);await advanceResult(actorB);await openHistory(actorB);assert.equal(await actorB.locator('.st-session-history__list button').count(),1);assert.notEqual(await output(actorB).getAttribute('data-session-id'),original)
  const narrow=await browser.newContext({viewport:{width:320,height:568},deviceScaleFactor:1});const english=await narrow.newPage();await english.goto(`${labUrl}?run=history&actor=qa-a&lang=en`);await waitIdle(english);await enter(english);await advanceResult(english);await openHistory(english);assert.equal(await english.locator('.st-session-history').getByText('Saved journeys').count(),1);assert.equal(await english.locator('.st-session-history__list button').count(),1);assert.equal(await english.evaluate(()=>document.documentElement.scrollWidth<=innerWidth),true);await english.locator('.st-session-history').evaluate(node=>node.scrollIntoView({block:'start'}));await english.screenshot({path:join(evidenceDirectory,`platform-layout-draw-me-out-session-history-en-320x568${evidenceSuffix}.png`),fullPage:true})
  const status=await (await fetch(`${origin}/__story_lab/status`)).json();assert.equal(status.liveModelCalled,false);assert.equal(status.liveMediaCalled,false);assert.equal(status.productionWrites,false);assert.equal(status.endingCalls,1);assert.equal(status.endingCommits.zh,1,'unknown ending response recovery commits exactly once');assert.equal(status.commits.zh,5);assert.equal(status.commits.en,1)
  await narrow.close();await wide.close();console.log(JSON.stringify({ok:true,liveModelCalled:false,liveMediaCalled:false,productionWrites:false,checks:['cinematic-result-before-decision','restart-retains-old-session','explicit-switch','unknown-turn-response-reload-recovery','recovered-result-phase','pending-ending-reload-recovery','one-ending-generation-and-commit','ending-keeps-ordinary-cursor','actor-isolation','locale-isolation','44px-rows','zh-390x844','en-320x568','no-overflow']}))
}finally{await browser?.close();await stopChild();await rm(databaseDirectory,{recursive:true,force:true})}
